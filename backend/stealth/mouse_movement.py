"""Placeholder: no synthetic mouse movement exists in this codebase.
`stealth.human`'s docstring explains why -- this engine never types or
clicks, only navigates and reads payloads, so faking pointer input would
add signal, not remove it. Reserved in case a future platform integration
requires actual UI interaction (not just navigation) to reach its data.
"""

from __future__ import annotations
