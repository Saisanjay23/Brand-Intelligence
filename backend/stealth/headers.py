"""Placeholder: no header manipulation beyond user-agent exists in this
codebase today. `fingerprint.UA` covers the User-Agent header; Playwright's
own default headers (Accept, Accept-Language, Accept-Encoding, etc.) are
used unmodified for every session. Reserved for if header-level spoofing
(e.g. Accept-Language per locale, Sec-CH-UA client hints) becomes necessary.
"""

from __future__ import annotations
