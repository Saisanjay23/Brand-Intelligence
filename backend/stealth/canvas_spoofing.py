"""Placeholder: canvas/WebGL/audio fingerprint spoofing is deliberately NOT
implemented -- see `stealth.navigator_spoofing`'s docstring. Those patches
are themselves detectable (overriding `HTMLCanvasElement.prototype` reads
as a privacy extension) and this engine has observed platforms respond
with degraded rendering or outright blocking sessions that carry them.
Reserved only in case that tradeoff is revisited for a specific platform.
"""

from __future__ import annotations
