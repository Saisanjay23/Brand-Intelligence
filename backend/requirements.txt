# Brand Intelligence Engine (headless) -- runtime dependencies

# browser automation (Facebook, X, Instagram)
playwright>=1.40

# API + service
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pydantic-settings>=2.5

# storage
motor>=3.3
pymongo>=4.6

# Telegram (MTProto)
telethon>=1.34

# better name matching; falls back to difflib without it
rapidfuzz>=3.6

# scheduled per-client discovery/analysis cron
apscheduler>=3.10

# outbound webhook callbacks to the calling platform
aiohttp>=3.9

# /metrics
prometheus-client>=0.20

# --- development only ---
pytest>=8.0
pytest-asyncio>=0.24
pyflakes>=3.2
